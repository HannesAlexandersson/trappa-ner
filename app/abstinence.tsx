import { Text, View } from "react-native";

export default function AbstinenceScreen() {
  // TODO: Add the 'fetch tips' logic that fetches the stored tips from DB
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text style={{ fontSize: 24 }}>Abstinence</Text>
    </View>
  );
}
